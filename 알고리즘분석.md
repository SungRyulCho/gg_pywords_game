# 워드 게임 알고리즘 분석

## 1. 문제 분석

단어 목록 파일(`data/word.txt`)을 읽어 리스트로 저장한다.  
저장된 단어 중 하나를 무작위로 화면에 제시하고, 사용자가 같은 단어를 입력했는지 비교한다.

게임은 총 5회 진행한다. 정답을 맞히면 정답 개수를 증가시키고 정답 소리를 재생한다. 틀리면 오답 소리를 재생한다.

게임이 끝나면 총 소요 시간, 맞힌 개수, 합격 여부를 출력한다. 맞힌 개수가 3개 이상이면 합격이고, 2개 이하이면 불합격이다. 게임 결과는 CSV 파일에 누적 저장한다.

## 2. 알고리즘

1. `random`, `time`, `pygame.mixer` 모듈을 불러온다.
2. `mixer.init()`으로 소리 재생 기능을 초기화한다.
3. `data/word.txt` 파일을 한 줄씩 읽는다.
4. 각 줄의 공백과 줄바꿈 문자를 제거한 뒤 `words` 리스트에 저장한다.
5. 게임 시작 시간을 저장하고, 정답 개수를 0으로 초기화한다.
6. 5회 반복하면서 `words` 리스트에서 임의의 단어를 하나 선택한다.
7. 선택한 단어를 화면에 출력하고 사용자에게 단어를 입력받는다.
8. 입력값과 제시 단어가 같으면 정답 수를 1 증가시키고 `good.wav`를 재생한다.
9. 다르면 오답을 출력하고 `bad.wav`를 재생한다.
10. 5회 반복이 끝나면 현재 시간에서 시작 시간을 빼서 게임 시간을 계산한다.
11. 맞힌 개수와 게임 시간을 출력한다.
12. 맞힌 개수가 3개 이상이면 합격, 아니면 불합격을 출력한다.
13. 게임 시간과 맞힌 개수를 `word_game_socre.csv` 파일에 추가한다.

## 3. 함수별 역할

- `wordLoad()`  
  `word.txt` 파일을 읽어 단어 리스트를 반환한다.

- `playSound(file_path)`  
  전달받은 소리 파일 경로의 WAV 파일을 재생한다.

- `gameRun(words)`  
  5회의 단어 입력 게임을 실행하고, 맞힌 개수와 걸린 시간을 반환한다.

- `scorePrint(score, elapsed_time)`  
  맞힌 개수, 걸린 시간, 합격 또는 불합격 결과를 출력한다.

- `saveResult(elapsed_time, score)`  
  게임 시간과 맞힌 개수를 CSV 파일에 누적 저장한다.
